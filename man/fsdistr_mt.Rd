% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsdistr.R
\name{fsdistr_mt}
\alias{fsdistr_mt}
\title{Final size distribution for a multi-type stochastic SIR epidemic}
\usage{
fsdistr_mt(
  s0,
  i0,
  beta,
  ip_model = "exponential",
  ip_params = list(1),
  return_df = FALSE
)
}
\arguments{
\item{s0}{Integer vector of length \code{m}. Initial susceptible counts per
group.}

\item{i0}{Integer vector of length \code{m}. Initial infective counts per
group. At least one element must be >= 1.}

\item{beta}{Numeric matrix of dimension \verb{m x m}. Entry \code{beta[i, j]} is
the rate at which an infective in group \code{i} contacts susceptibles in
group \code{j}.}

\item{ip_model}{Character vector of length \code{m} (or scalar, recycled).
Infectious period model per group: \code{"constant"}, \code{"exponential"}, or
\code{"gamma"}.}

\item{ip_params}{List of length \code{m} (or length 1, recycled). Each element
is the parameter(s) for the infectious period distribution of the
corresponding group.}
}
\value{
A named numeric vector of length \code{prod(s0 + 1)}. Each element
gives the probability of the corresponding joint outcome, with names
encoding the outcome as \code{"k1-k2-...-km"}.
}
\description{
Computes the exact joint probability distribution of the number infected
in each group during an outbreak, for a population with multiple
susceptible groups and heterogeneous mixing. Based on equation 3.4 in
Ball (1986).
}
\examples{
# Two-group model
beta2 <- matrix(c(1.5, 0.5, 0.5, 1.5), nrow = 2)
fsdistr_mt(s0 = c(5, 5), i0 = c(1, 0), beta = beta2)

# Single group (should match fsdistr())
fsdistr_mt(s0 = 6, i0 = 1, beta = matrix(1.4), ip_model = "constant",
           ip_params = list(1 / 0.9))

}
\references{
Ball, F. (1986). A unified approach to the distribution of total size and
total area under the trajectory of infectives in epidemic models.
\emph{Advances in Applied Probability}, 18(2), 289–310.
}
