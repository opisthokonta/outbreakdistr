% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emm.R
\name{emmdt}
\alias{emmdt}
\alias{emmct}
\alias{emmct_sis}
\title{Epidemic Markov Models}
\usage{
emmdt(s0, i0, beta, gamma, sparse = TRUE)

emmct(s0, i0, beta, gamma, time, beta0 = 0, sparse = TRUE, expm_method = NULL)

emmct_sis(
  s0,
  i0,
  gamma,
  beta,
  time,
  beta0 = 0,
  sparse = TRUE,
  expm_method = NULL
)
}
\arguments{
\item{s0}{Positive integer. Initial number of susceptibles.}

\item{i0}{Positive integer. Initial number of infectives (must be >= 1).}

\item{beta}{Non-negative numeric. Infection rate parameter. The per-step
infection probability is proportional to \code{beta * S * I / s0}.}

\item{gamma}{Non-negative numeric. Recovery rate parameter. The per-step
recovery probability is proportional to \code{gamma * I}.}

\item{sparse}{Logical. If \code{TRUE} (default), use a sparse matrix
representation via the Matrix package; otherwise use a dense matrix.
Sparse matrices are faster for large populations.}

\item{time}{Positive numeric. The time to evaluate the the model, assuming an initial state at time 0 defined by \code{s0} and \code{i0}.}

\item{beta0}{Non-negative numeric. Rate of infection from outside the population.}
}
\value{
A list with the following elements:
\describe{
\item{\code{transition_matrix}}{The full one-step transition matrix.}
\item{\code{qmat}}{Sub-matrix of transitions among transient states
(canonical form).}
\item{\code{rmat}}{Sub-matrix of transitions from transient to absorbing
states (canonical form).}
\item{\code{fmat}}{Fundamental matrix \code{(I - Q)^{-1}}.}
\item{\code{solution_mat}}{Absorption probability matrix: entry \verb{[i, j]} is
the probability of being absorbed in state \code{j} when starting in
transient state \code{i}.}
\item{\code{fs_distr}}{Numeric vector of length \code{s0 + 1}. Element \code{k + 1}
gives the probability that exactly \code{k} susceptibles are infected
(k = 0, 1, ..., s0).}
}
}
\description{
Exact computation for Epidemic Markov Models. Use the \code{emmct} for continuous time
modeling of a SIR epidemic, use \code{emmct_sis} for epidemics with a SIS dynamic.
\code{emmdt} is the discrete time variant of the SIR epidemic, which is mostly useful
for computing the final size distribution. The models are described in detail in
the Epidemic Markov Models vignette.
}
\examples{
res <- emmdt(s0 = 10, i0 = 1, beta = 1.5, gamma = 1.0)
res$fs_distr

# Dense matrix representation
res_dense <- emmdt(s0 = 10, i0 = 1, beta = 1.5, gamma = 1.0, sparse = FALSE)

}
